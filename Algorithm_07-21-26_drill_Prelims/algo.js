// Kung gwapa/gwapo, buot kag alam siya,
// cge, gowers ta kag istoryahon.
// Pero kung may kulang bisan isa,
// ind bet.

function crush() {
    let gwapa = true;   // or gwapo
    let buot = true;
    let alam = true;

    if (gwapa && buot && alam) {
        console.log("Cge, gowers! Istoryaha siya.");
    } else {
        console.log("Ind bet. Okay man siya, pero pass lang anay.");
    }
}

crush();